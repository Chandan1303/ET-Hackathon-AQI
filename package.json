{
  "name": "urban-eco-os",
  "version": "1.0.0",
  "description": "Urban Eco OS - Environmental Decision Intelligence Platform",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "npx concurrently --kill-others \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:ai\"",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "start:ai": "cd ai-service && venv\\Scripts\\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
